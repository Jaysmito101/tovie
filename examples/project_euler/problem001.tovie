proc_main
	0 1 
	while
		1 call
		1 +
		dup 1000 < 
	while_end
	pop
	print
proc_end

proc_1
	dup 3 % 0 == memset_0
	dup 5 % 0 == memset_1
	memget_0 memget_1 || if
		memset_0
		memget_0
		+
		memget_0
	if_end
proc_end
